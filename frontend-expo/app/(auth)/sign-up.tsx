import { View } from "react-native";
import { Text } from "react-native-paper";

export default function SigUp() {
    return (
        <View>
            <Text variant="titleLarge" theme={{ colors: { primary: "red" } }}>Pagina SigUp</Text>
        </View>
    )
}